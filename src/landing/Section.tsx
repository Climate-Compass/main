import React from 'react';

type sectionType = {
    display: string;
};

const section: React.FC<sectionType> = () => {
    return (
        <section>

        </section>
    );
}
export default section;